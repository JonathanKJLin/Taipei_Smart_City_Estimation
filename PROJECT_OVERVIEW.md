# 政府估驗計價自動化驗算系統 - 專案總覽

## 📋 專案狀態

**目前階段**: 架構設計與框架建立  
**開發狀態**: ✅ 框架完成，⚠️ 待實際文件範本後實作具體邏輯  
**版本**: 0.1.0 (Alpha)  
**建立日期**: 2026-01-12

---

## 🎯 專案目標

建立一個智能化的估驗計價文件處理與驗算系統，透過以下技術實現自動化：

1. **Azure Document Intelligence** - 文件掃描與 ICR（智能字元識別）
2. **Azure OpenAI GPT-5** - 自然語言處理與語義理解
3. **AI 規則引擎** - 自動執行金額驗算、累計檢核、付款條件驗證
4. **標準化輸出** - JSON 格式的結構化資料
5. **持續學習** - 從回饋中優化 AI 模型

---

## 🏗️ 技術架構

### 技術棧
- **後端框架**: Django 4.x
- **AI 服務**: 
  - Azure Document Intelligence (文件掃描)
  - Azure OpenAI GPT-5 (語義理解)
- **異步處理**: Celery + Redis
- **資料庫**: PostgreSQL
- **語言**: Python 3.10+

### GPT-5 配置位置

GPT-5 已配置在以下位置：

1. **環境變數設定** (需手動配置 `.env` 檔案):
   ```bash
   AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
   AZURE_OPENAI_KEY=your-key
   AZURE_OPENAI_DEPLOYMENT_NAME=gpt-5  # ← GPT-5 部署名稱
   AZURE_OPENAI_API_VERSION=2024-12-01-preview
   ```

2. **Django 設定檔** (`django_app/settings/base.py`):
   ```python
   # Line 83-86
   AZURE_OPENAI_DEPLOYMENT_NAME = os.getenv('AZURE_OPENAI_DEPLOYMENT_NAME', 'gpt-5')
   ```

3. **GPT 服務類別** (`django_app/services/base/azure_gpt_service.py`):
   ```python
   # Line 25-27
   self.deployment_name = settings.AZURE_OPENAI_DEPLOYMENT_NAME  # 使用 GPT-5
   ```

---

## 📁 專案結構

```
gov_estimation_system/
├── django_app/                      # Django 應用
│   ├── apps/                        # 應用模組
│   │   ├── document_processor/      # 文件處理器 (✅ 完成)
│   │   └── estimation_validator/    # 驗證器 (✅ 完成)
│   │
│   ├── services/                    # 服務層
│   │   ├── base/                    # 基礎服務 (✅ 可重用)
│   │   │   ├── azure_di_service.py      # Azure DI
│   │   │   ├── azure_gpt_service.py     # GPT-5 服務
│   │   │   ├── pdf_handler.py           # PDF 工具
│   │   │   └── confidence_calculator.py # 信心分數
│   │   │
│   │   ├── estimation/              # 估驗計價服務 (⚠️ 待完善)
│   │   │   ├── amount_engine.py             # 金額驗算
│   │   │   ├── accumulation_checker.py      # 累計檢核
│   │   │   ├── payment_condition_engine.py  # 付款條件
│   │   │   └── rules_engine.py              # 規則引擎
│   │   │
│   │   └── templates/               # 模板
│   │       ├── prompt/              # GPT Prompt (⚠️ 待優化)
│   │       └── json_format/         # JSON 範例
│   │
│   ├── schemas/                     # Schema 定義 (⚠️ 待完善)
│   ├── settings/                    # Django 設定 (✅ 完成)
│   └── utils/                       # 工具函數 (✅ 完成)
│
├── data/                            # 資料目錄
│   ├── sample_documents/            # 範例文件 (空，待加入)
│   ├── processed/                   # 處理結果
│   ├── results/                     # 驗算結果
│   └── uploads/                     # 上傳文件
│
├── requirements.txt                 # Python 依賴 (✅ 完成)
├── README.md                        # 專案說明 (✅ 完成)
├── DEVELOPMENT_GUIDE.md             # 開發指南 (✅ 完成)
└── PROJECT_OVERVIEW.md              # 本文件
```

---

## ✅ 已完成的工作

### 1. 專案架構設計
- ✅ 完整的目錄結構
- ✅ Django 應用配置
- ✅ 開發與正式環境設定分離

### 2. 基礎服務層（可重用）
- ✅ Azure Document Intelligence 服務封裝
- ✅ Azure OpenAI GPT-5 服務封裝
- ✅ PDF 處理工具
- ✅ 信心分數計算器
- ✅ 檔案處理工具

### 3. 核心業務模組（框架）
- ✅ 金額驗算引擎框架
- ✅ 累計檢核模組框架
- ✅ 付款條件引擎框架
- ✅ 規則引擎與學習模組框架

### 4. Django 應用
- ✅ Document Processor App (文件處理)
  - Models: Document, ProcessingLog
  - Views: 文件上傳、查詢、重新處理
  - Tasks: 異步處理任務
  - Admin: 後台管理介面
  
- ✅ Estimation Validator App (驗證器)
  - Models: ValidationRule, FeedbackRecord
  - Views: 規則管理、回饋提交
  - Admin: 後台管理介面

### 5. 模板與 Schema
- ✅ Prompt 模板框架
  - field_extraction.txt (欄位擷取)
  - logic_identification.txt (邏輯識別)
  - payment_condition_parsing.txt (付款條件解析)
- ✅ JSON Schema 定義框架
- ✅ JSON 格式範例

### 6. 文件
- ✅ README.md (專案說明)
- ✅ DEVELOPMENT_GUIDE.md (開發指南)
- ✅ PROJECT_ARCHITECTURE.md (架構介紹)
- ✅ COMPARISON_AZTL_vs_GOVERNMENT.md (專案對比)
- ✅ TECHNICAL_OVERVIEW.md (技術概覽)

---

## ⚠️ 待完成的工作（需要實際文件範本）

### 1. Schema 完善
- [ ] 根據實際文件結構調整 JSON Schema
- [ ] 確定所有必要欄位與資料型態
- [ ] 定義驗算結果的詳細結構

### 2. Prompt 優化
- [ ] 根據實際文件優化 Prompt 模板
- [ ] 收集 Few-shot Learning 範例
- [ ] 測試並調整 Prompt 效果

### 3. 驗算規則實作
- [ ] **金額驗算引擎**
  - [ ] 實作直式加總具體邏輯
  - [ ] 實作橫式計算具體邏輯
  - [ ] 定義誤差容忍範圍
  
- [ ] **累計檢核模組**
  - [ ] 實作累計邏輯檢核
  - [ ] 實作合約上限檢查
  - [ ] 實作進度合理性分析
  
- [ ] **付款條件引擎**
  - [ ] 優化條件解析邏輯
  - [ ] 擴充條件類型識別
  - [ ] 實作條件驗證邏輯

### 4. 規則學習機制
- [ ] 實作規則自動提取
- [ ] 實作從回饋中學習
- [ ] 建立規則庫管理系統

### 5. 測試與優化
- [ ] 單元測試
- [ ] 整合測試
- [ ] 效能測試與優化
- [ ] 準確度評估

### 6. 前端開發
- [ ] 文件上傳介面
- [ ] 處理進度顯示
- [ ] 驗算結果展示
- [ ] 人工審核介面
- [ ] 回饋提交介面

---

## 🚀 快速開始

### 1. 環境準備

```bash
# 克隆專案（如果是 Git 管理）
cd "/Users/linkaijun/Desktop/EY/台北智慧城市/gov_estimation_system"

# 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安裝依賴
pip install -r requirements.txt
```

### 2. 配置環境變數

手動建立 `.env` 檔案（因為 .env.example 被 gitignore 阻擋）：

```bash
# 創建 .env 檔案
cat > .env << 'EOF'
# Azure Document Intelligence
AZURE_DI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DI_KEY=your-key

# Azure OpenAI GPT-5
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-5
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Database
DB_NAME=gov_estimation_db
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_URL=redis://localhost:6379/0

# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Application
MAX_UPLOAD_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,png,jpg,jpeg,tiff
CONFIDENCE_THRESHOLD=0.7
EOF
```

### 3. 資料庫設定

```bash
# 執行 migrations
cd django_app
python manage.py makemigrations
python manage.py migrate

# 建立超級使用者
python manage.py createsuperuser
```

### 4. 啟動服務

```bash
# 啟動 Django 開發伺服器
python manage.py runserver

# 另開終端，啟動 Celery Worker
celery -A django_app worker -l info
```

### 5. 存取應用

- **Django Admin**: http://localhost:8000/admin/
- **API 文件**: http://localhost:8000/api/
- **Health Check**: http://localhost:8000/health/

---

## 📊 核心工作流程

```
文件上傳
    ↓
Azure DI 掃描 (ICR 智能字元識別 + 表格識別)
    ↓
GPT-5 語義理解 (欄位對應 + 邏輯識別)
    ↓
資料標準化 (轉換為 JSON)
    ↓
AI 規則引擎驗算
    ├── 金額加總檢查
    ├── 累計金額檢核
    └── 付款條件驗算
    ↓
生成驗算結果報告
    ↓
人工審核與回饋
    ↓
AI 模型持續優化
```

---

## 🔑 關鍵設計決策

### 1. 為什麼選擇 GPT-5？
- 更強的語義理解能力
- 更好的邏輯推理能力
- 支援更複雜的 Prompt Engineering

### 2. 為什麼使用模組化架構？
- **可重用性**: base 服務可用於其他專案
- **可維護性**: 清晰的職責劃分
- **可擴展性**: 易於添加新功能

### 3. 為什麼採用異步處理？
- 文件處理耗時較長
- 避免阻塞 API 回應
- 支援大量文件並發處理

### 4. 為什麼需要回饋機制？
- 持續優化 AI 準確度
- 學習新的文件格式
- 動態調整驗算規則

---

## 📝 重要注意事項

### 1. 目前的限制
- ⚠️ 所有驗算規則都是框架，需要實際文件範本後實作
- ⚠️ Prompt 模板需要根據實際文件優化
- ⚠️ JSON Schema 需要根據實際需求調整
- ⚠️ 信心分數計算目前使用簡化邏輯

### 2. 開發優先級
1. **第一優先**: 取得實際文件範本，分析結構
2. **第二優先**: 完善 Schema 和 Prompt
3. **第三優先**: 實作具體驗算規則
4. **第四優先**: 測試與優化

### 3. 環境依賴
- **必須**: Azure Document Intelligence 訂閱
- **必須**: Azure OpenAI GPT-5 訂閱
- **必須**: PostgreSQL 資料庫
- **必須**: Redis 服務
- **可選**: Azure Blob Storage (用於文件儲存)

---

## 🤝 團隊協作

### 角色分工建議
- **專案經理**: 整體規劃與客戶溝通
- **AI 工程師**: GPT Prompt 優化、模型訓練
- **後端工程師**: 業務邏輯實作、API 開發
- **前端工程師**: 使用者介面開發
- **領域專家**: 估驗計價業務邏輯諮詢
- **測試工程師**: 測試與品質保證

### 協作流程
1. **需求確認**: 與政府單位確認文件格式與驗算需求
2. **範本收集**: 收集各類估驗計價文件範本
3. **開發實作**: 根據範本實作具體邏輯
4. **測試驗證**: 使用實際文件測試
5. **部署上線**: 部署到正式環境
6. **維運優化**: 持續收集回饋並優化

---

## 📞 聯絡資訊

**專案名稱**: 政府估驗計價自動化驗算系統  
**客戶單位**: 台北市政府相關單位  
**開發團隊**: EY 安永聯合會計師事務所  
**專案狀態**: Alpha 階段 (架構完成，待實際範本)

---

## 📚 相關文件

- **README.md** - 專案基本說明與快速開始
- **DEVELOPMENT_GUIDE.md** - 詳細的開發指南與 API 文件
- **PROJECT_ARCHITECTURE.md** - 系統架構完整介紹
- **TECHNICAL_OVERVIEW.md** - 技術架構速查表
- **COMPARISON_AZTL_vs_GOVERNMENT.md** - 與 AZTL 專案對比分析

---

*最後更新: 2026-01-12*  
*版本: 0.1.0 (Alpha)*
